.expressions-table-actions
  %button.download{type: 'button'} Get CSV
  %input{type: "text", value: 'expressions.csv', class: 'csv-filename'}
%table.gene_expression_by_tissue.tablesorter{'data-sortlist' => '[[1,1]]'}
  %thead
    %tr
      %th Sample
      %th{'data-placeholder' => "Try `>=100` or `100 to 1000`", 'data-value' => '>10'} Expression
      %th.sorter-false.ignore_csv_output.expression_bar Expression bar
  %tbody
    - expressions_by_tissues.each do |tissue, expression|
      %tr
        %td= tissue
        %td= expression.round(1)
        %td= 100 * expression.to_f / statistics.max
